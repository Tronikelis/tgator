services:
  postgres:
    image: "postgres:15"
    environment:
      POSTGRES_USER: "u"
      POSTGRES_PASSWORD: "p"
      POSTGRES_DB: "db"
    ports:
      - "5432:5432"
    volumes:
      - "./tmp/pg:/var/lib/postgresql/data"
